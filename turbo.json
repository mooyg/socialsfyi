{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
    "build:schema": {},
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build:schema"]
    }
  }
}
